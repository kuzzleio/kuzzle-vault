{
  "name": "kuzzle-vault",
  "version": "2.0.0",
  "description": "Encrypt and decrypt secrets with Kuzzle Vault",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "npm run --silent lint && npm run unit-testing",
    "test:unit": "nyc --reporter=text-summary --reporter=lcov mocha",
    "codecov": "codecov",
    "test:lint": "eslint . --max-warnings=0 --ext .ts --config .eslintrc",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kuzzleio/kuzzle-vault.git"
  },
  "author": "The Kuzzle Team <support@kuzzle.io>",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/kuzzleio/kuzzle-vault/issues"
  },
  "homepage": "https://github.com/kuzzleio/kuzzle-vault#readme",
  "dependencies": {},
  "devDependencies": {
    "@types/mocha": "^7.0.2",
    "@types/mock-fs": "^4.10.0",
    "@types/should": "^13.0.0",
    "@types/sinon": "^7.5.2",
    "mock-fs": "^4.11.0",
    "ts-node": "^8.8.1",
    "@types/node": "^10.0.3",
    "codecov": "^3.5.0",
    "eslint": "^6.3.0",
    "gts": "^1.1.2",
    "mocha": "^6.2.0",
    "nyc": "^14.1.1",
    "should": "^13.2.3",
    "should-sinon": "0.0.6",
    "sinon": "^7.4.2",
    "typescript": "~3.7.0"
  }
}
